{% extends 'base.html.twig' %}

{% block title %}Crée un serveurs{% endblock %}

{% block body %}
    <div class="container">
        <h1>crée un serveurs </h1>
        {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit" class="btn btn-primary">Crée</button>
        {{ form_end(form) }}
    </div>
{% endblock %}